<Grid x:Class="Procon.UI.Default.Root.Main.Header.HeaderView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:conv="clr-namespace:Procon.UI.API.Converters;assembly=Procon.UI.API"
      xmlns:view="clr-namespace:Procon.UI.API.ViewModels;assembly=Procon.UI.API"
      xmlns:acmd="clr-namespace:Procon.UI.API.Commands;assembly=Procon.UI.API"
      xmlns:util="clr-namespace:Procon.UI.API.Utils;assembly=Procon.UI.API"
      Name="MainHeader">
    <Grid.Resources>
        <conv:TextLocalizationConverter x:Key="TextLoc" />
        <conv:EnumLocalizationConverter x:Key="EnumLoc" />
    </Grid.Resources>

    <!-- Content -->
    <DockPanel Name="MainHeaderContent"
               Height="38">
        <Button Name="MainHeaderSwap"
                DockPanel.Dock="Left"
                Width="48"
                Command="{Binding
                    Source={x:Static view:ViewModelBase.PublicCommands},
                    Path=[Main][Header][Swap].Value}">
            <Image Width="30"
                   Source="{Binding
                       Source={x:Static view:ViewModelBase.PublicProperties},
                       Path=[Images][Connection][Swap].Value}" />
        </Button>
        <Image Name="MainHeaderLogo"
               DockPanel.Dock="Right"
               Margin="0, 0, 10, 0"
               Source="{Binding
                    Source={x:Static view:ViewModelBase.PublicProperties},
                    Path=[Images][Procon][Small].Value}" />
        <DockPanel Name="MainHeaderSubContent"
                   Margin="15, 7">
            <Image Name="MainHeaderIcon"
                   DockPanel.Dock="Left"
                   Margin="0, 0, 10, 0"
                   Source="{Binding
                       Source={x:Static view:ViewModelBase.PublicProperties},
                       Path=[Images][Connection][Info].Value}"/>
            <Label Name="MainHeaderName"
                   DockPanel.Dock="Left"
                   Margin="0, 0, 10, 0"
                   Content="{Binding ServerName}" />
            <Label Name="MainHeaderPadding" Content=" "/>
        </DockPanel>
    </DockPanel>
</Grid>
