<Grid x:Class="Procon.UI.Default.Root.Main.Players.List.ListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:mcon="clr-namespace:Procon.UI.Default.Converters"
      xmlns:conv="clr-namespace:Procon.UI.API.Converters;assembly=Procon.UI.API"
      xmlns:view="clr-namespace:Procon.UI.API.ViewModels;assembly=Procon.UI.API"
      xmlns:acmd="clr-namespace:Procon.UI.API.Commands;assembly=Procon.UI.API"
      xmlns:util="clr-namespace:Procon.UI.API.Utils;assembly=Procon.UI.API"
      Name="MainPlayersList">
    <Grid.Resources>
        <conv:TextLocalizationConverter x:Key="TextLoc"     />
        <conv:EnumLocalizationConverter x:Key="EnumLoc"     />
        <conv:CountryConverter          x:Key="CountryConv" />
        <mcon:ScoreConverter            x:Key="ScoreConv"   />
        <mcon:KdrConverter              x:Key="KdrConv"     />
        <mcon:PingConverter             x:Key="PingConv"    />
        
        <Color           x:Key="ColorTeam1Dark"  A="255" R="124" G="207" B="224" />
        <Color           x:Key="ColorTeam1Light" A="255" R="178" G="230" B="239" />
        <Color           x:Key="ColorTeam2Dark"  A="255" R="255" G="160" B="148" />
        <Color           x:Key="ColorTeam2Light" A="255" R="255" G="202" B="194" />
        <SolidColorBrush x:Key="BrushTeam1Dark"  Color="{StaticResource ColorTeam1Dark}"  />
        <SolidColorBrush x:Key="BrushTeam1Light" Color="{StaticResource ColorTeam1Light}" />
        <SolidColorBrush x:Key="BrushTeam2Dark"  Color="{StaticResource ColorTeam2Dark}"  />
        <SolidColorBrush x:Key="BrushTeam2Light" Color="{StaticResource ColorTeam2Light}" />
        
        <DataTemplate DataType="{x:Type view:PlayerViewModel}">
            <ToggleButton x:Name="PART_Content"
                          Width="190"
                          Margin="5, 5, 0, 0"
                          Padding="0"
                          BorderThickness="0"
                          Command="{Binding
                              Source={x:Static view:ViewModelBase.PublicCommands},
                              Path=[Main][Players][List][Select].Value}"
                          CommandParameter="{Binding}">
                <ToggleButton.Style>
                    <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource {x:Type ToggleButton}}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                    <Border x:Name="PART_Border"
                                            Margin="{TemplateBinding Margin}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            VerticalAlignment="{TemplateBinding VerticalAlignment}"
                                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                            CornerRadius="0">
                                        <ContentPresenter x:Name="PART_Content"
                                                          Width="{TemplateBinding Width}"
                                                          Height="{TemplateBinding Height}"
                                                          Margin="{TemplateBinding Padding}" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ToggleButton.Style>
                <DockPanel x:Name="PART_Container">
                    
                    <!-- Title Portion -->
                    <DockPanel x:Name="PART_Title"
                               DockPanel.Dock="Top">
                        <Image x:Name="PART_Icon"
                               DockPanel.Dock="Left"
                               Height="16"
                               Margin="4"
                               Source="{Binding
                                   Source={x:Static view:ViewModelBase.PublicProperties},
                                   Path=[Images][General][Player].Value}" />
                        <Image x:Name="PART_Country"
                               DockPanel.Dock="Right"
                               Height="16"
                               Margin="4, 4, 10, 4"
                               DataContext="{Binding CountryCode}"
                               Source="{Binding Converter={StaticResource CountryConv}}" />
                        <CheckBox x:Name="PART_Selected"
                                  DockPanel.Dock="Right"
                                  VerticalAlignment="Center"
                                  IsChecked="{Binding Path=IsChecked, ElementName=PART_Content}" />
                        <TextBlock x:Name="PART_Name"
                                   DockPanel.Dock="Left"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   Text="{Binding Name}" />
                    </DockPanel>
                    
                    <!-- Data Portion -->
                    <Grid x:Name="PART_Data"
                          DockPanel.Dock="Top">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="3" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="3" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="1.0*" />
                        </Grid.ColumnDefinitions>
                    
                        <Image x:Name="PART_ScoreWarn"
                               Grid.Row="1"
                               Height="16"
                               Margin="4, 0"
                               DataContext="{Binding Score}"
                               Source="{Binding Converter={StaticResource ScoreConv}}" />
                        <Image x:Name="PART_KdrWarn"
                               Grid.Row="2"
                               Height="16"
                               Margin="4, 0"
                               DataContext="{Binding Kdr}"
                               Source="{Binding Converter={StaticResource KdrConv}}" />
                        <Image x:Name="PART_PingWarn"
                               Grid.Row="3"
                               Height="16"
                               Margin="4, 0"
                               DataContext="{Binding Ping}"
                               Source="{Binding Converter={StaticResource PingConv}}" />
                    
                        <TextBlock x:Name="PART_Score"
                                   Grid.Row="1"
                                   Grid.Column="1"
                                   VerticalAlignment="Center"
                                   FontSize="11"
                                   Text="{Binding
                                       StringFormat=Score: {0:0},
                                       Path=Score}" />
                        <TextBlock x:Name="PART_Kdr"
                                   Grid.Row="2"
                                   Grid.Column="1"
                                   VerticalAlignment="Center"
                                   FontSize="11"
                                   Text="{Binding
                                       StringFormat=Kdr: {0:0.00},
                                       Path=Kdr}" />
                        <TextBlock x:Name="PART_Ping"
                                   Grid.Row="3"
                                   Grid.Column="1"
                                   VerticalAlignment="Center"
                                   FontSize="11"
                                   Text="{Binding
                                       StringFormat=Ping: {0:0},
                                       Path=Ping}" />
                    </Grid>
                </DockPanel>
            </ToggleButton>
                
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Team}" Value="Team1">
                    <Setter Property="Background" Value="{StaticResource BrushTeam1Dark}" TargetName="PART_Title" />
                    <Setter Property="Background" Value="{StaticResource BrushTeam1Light}" TargetName="PART_Data" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Team}" Value="Team2">
                    <Setter Property="Background" Value="{StaticResource BrushTeam2Dark}" TargetName="PART_Title" />
                    <Setter Property="Background" Value="{StaticResource BrushTeam2Light}" TargetName="PART_Data" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Grid.Resources>
    
    <!-- Content -->
    <DockPanel Name="MainPlayersListContent">
        
        <!-- Title -->
        <Grid Name="MainPlayersListTitle"
              DockPanel.Dock="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="1.0*" />
            </Grid.ColumnDefinitions>
            
            <Label Name="MainPlayersListName"
                   Grid.Row="0"
                   Grid.Column="0"
                   Margin="15, 15, 15, 0"
                   FontSize="26"
                   Content="{Binding
                       Converter={StaticResource TextLoc},
                       ConverterParameter=Procon.UI.Default.Root.Main.Players.Title}" />
            <Label Name="MainPlayersListMode"
                   Grid.Row="1"
                   Grid.Column="0" 
                   Margin="15, 0, 15, 15"
                   FontSize="14"
                   Content="{Binding
                       Source={x:Static view:ViewModelBase.PublicProperties},
                       Path=[Connection].Value.GameMode,
                       FallbackValue='[???]'}" />
            <TextBlock Name="MainPlayersListCount"
                       Grid.Row="0"
                       Grid.Column="1"
                       Margin="0, 15, 0, 0"
                       FontSize="15"
                       VerticalAlignment="Center">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0}/{1}">
                        <Binding Path="PlayerCount"    FallbackValue="?" />
                        <Binding Path="MaxPlayerCount" FallbackValue="?" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            
            <!-- Player Statistics -->
            <StackPanel Name="MainPlayersTitleStats"
                        Grid.Row="0"
                        Grid.RowSpan="2"
                        Grid.Column="2"
                        Margin="0, 0, 15, 0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <Label Content="Score Thingy Goes Here." Margin="10, 0" />
                <Label Content="KDR Thingy Goes Here."   Margin="10, 0" />
                <Label Content="Ping Thingy Goes Here."  Margin="10, 0" />
            </StackPanel>
            
        </Grid>
        
        <!-- Actions -->
        <DockPanel Name="MainPlayersListActions"
                   DockPanel.Dock="Top">

            <Button Name="MainPlayersListBan"
                    DockPanel.Dock="Left"
                    MinWidth="80"
                    Padding="0, 5"
                    Margin="15, 10, 0, 10"
                    Command="{Binding
                        Source={x:Static view:ViewModelBase.PublicCommands},
                        Path=[Main][Players][List][Ban].Value}"
                    Content="{Binding
                        Converter={StaticResource TextLoc},
                        ConverterParameter=Procon.UI.Default.Root.Main.Players.Ban}" />
            <Button Name="MainPlayersListKick"
                    DockPanel.Dock="Left"
                    MinWidth="80"
                    Padding="0, 5"
                    Margin="10, 10, 0, 10"
                    Command="{Binding
                        Source={x:Static view:ViewModelBase.PublicCommands},
                        Path=[Main][Players][List][Kick].Value}"
                    Content="{Binding
                        Converter={StaticResource TextLoc},
                        ConverterParameter=Procon.UI.Default.Root.Main.Players.Kick}" />
            
            
            <RadioButton Name="MainPlayersListListView"
                         DockPanel.Dock="Right"
                         Margin="0, 10, 15, 10"
                         Command="{Binding
                             Source={x:Static view:ViewModelBase.PublicCommands},
                             Path=[Main][Players][List][ListView].Value}"
                         Content="{Binding
                             Source={x:Static view:ViewModelBase.PublicProperties},
                             Path=[Images][Players][ListView].Value}" />
            <RadioButton Name="MainPlayersListGridView"
                         DockPanel.Dock="Right"
                         Margin="0, 10, 10, 10"
                         Command="{Binding
                             Source={x:Static view:ViewModelBase.PublicCommands},
                             Path=[Main][Players][List][GridView].Value}"
                         Content="{Binding
                             Source={x:Static view:ViewModelBase.PublicProperties},
                             Path=[Images][Players][GridView].Value}" />
            <ToggleButton Name="MainPlayersListHighlight"
                          DockPanel.Dock="Right"
                          Margin="0, 10, 10, 10"
                          Command="{Binding
                              Source={x:Static view:ViewModelBase.PublicCommands},
                              Path=[Main][Players][List][Highlight].Value}"
                          Content="{Binding
                              Source={x:Static view:ViewModelBase.PublicProperties},
                              Path=[Images][Players][Highlight].Value}" />
            
            <Label Name="MainPlayersListPadding" Content=" "/>
        </DockPanel>
        
        <!-- Players -->
        <ItemsControl Name="MainPlayersListList"
                      DockPanel.Dock="Bottom"
                      ItemsSource="{Binding Players}">
            <ItemsControl.Template>
                <ControlTemplate>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <WrapPanel Width="{TemplateBinding Width}"
                                   Height="{TemplateBinding Height}"
                                   Margin="5"
                                   IsItemsHost="True" />
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>
        </ItemsControl>
        
    </DockPanel>
</Grid>
